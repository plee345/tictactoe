#include <iostream>
#include <vector>
#include <string>

std::string checkPosition(std::vector<std::vector<std::string>>& board);
std::string winCondition(std::string& move);


int main(){
    bool win = false;
    std::vector<std::vector<int>>{
        "000", "000", "000"
    };
    std::cout << "Let's play tic-tac-toe!" << std::endl;
    while (!win){
        std::cout << "Please make a valid move." << std::endl;
        bool valid = false;
        while (!valid){
            
        }
    }
    return 0;
}

std::string checkPosition(std::vector<std::vector<std::string>>& board){
    
}

std::string winCondition(std::string& move){
    
}